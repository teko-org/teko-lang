# kind = print checks BOTH the exit code and stdout. args are passed to the binary's argv
# (the runner's plumbing) even though this base program does not read them.
args = ["7", "13", "5"]
kind = "print"
exit = 3
stdout = "ok"
