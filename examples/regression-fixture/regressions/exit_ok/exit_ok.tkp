# A passing regressive: it prints "ok" and exits 3. The `.tkr` still PASSES three argv
# operands (exercising the runner's arg-passing plumbing) and checks stdout + exit. The
# fold-opaque-operand programs are 0.3.0.30; this base fixture proves the mechanism.
name = "exit_ok"
source = "src"

[artifact]
kind = "binary"
