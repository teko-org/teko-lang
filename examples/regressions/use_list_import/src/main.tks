use use_list_import::util::[add, mul]

let sum = use_list_import::util::add(3, 4)
if sum != 7 { exit(1) }

let product = use_list_import::util::mul(sum, 3)
if product != 21 { exit(2) }

exit(product)
