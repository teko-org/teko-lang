# Regression (#517, negative): a range loop's counter `i` is LOOP-PRIVATE (typed in a child scope);
# reading it AFTER the loop must be REJECTED (the init/body bindings do not leak). No successful build.
name = "loop_range_counter_leak"
source = "src"

[artifact]
kind = "binary"
