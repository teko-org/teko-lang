Negative regression (A): a per-element out-of-range literal in an annotated slice fails early (M.1).
(A) borns each fitting element at the element width but never relaxes the per-element range check, so
`let xs: []u8 = [1, 300, 3]` stays a compile-time error (300 does not fit u8).
EXPECT_STDERR: literal out of range for the annotated type (M.1 — fail early)
