This fixture is a NEGATIVE regression (#517, owner ruling): `_ as x` is a semantic contradiction —
`_` is a discard (ignore this value), `as x` is a capture (bind it as x) — you cannot re-capture
what you discarded. The parser rejects `_ as <name>` (name != `_`) OUTRIGHT, at parse time, with a
clear diagnostic ("`_` is a discard and cannot be bound to a name"). `_ as _` (discard-as-discard)
remains valid. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
