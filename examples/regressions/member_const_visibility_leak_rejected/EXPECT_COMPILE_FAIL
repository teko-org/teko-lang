This fixture is a NEGATIVE regression: the checker MUST reject it (a `private`
static member const, inherited by name through a subclass declared in a
DIFFERENT namespace, referenced from that subclass's own namespace — the const's
DECLARING namespace, not the subclass's, gates `private` reach; D6). It has no
successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir
containing this marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
