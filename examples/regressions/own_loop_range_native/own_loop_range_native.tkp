# F2 own-backend differential fixture (issue #389 §6.2). Mirrors loop_range's range-loop
# self-checks — the case the regalloc loop back-edge honest-stop refused before F2. Exercises
# exclusive/inclusive endpoints, `continue` running the step, and once-captured bounds, all
# exit(n)-scoped so VM, C-native, and own-native must agree on exit 0 (scripts/diff_c_own.sh).
name = "own_loop_range_native"
source = "src"

[artifact]
kind = "binary"
