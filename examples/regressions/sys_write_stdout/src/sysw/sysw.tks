/** sysw::run — write "hi\n" to stdout (fd 1) through the raw SYS_write syscall, no libc; returns 0. */
pub fn run(): i64 {
    var msg = "hi\n"
    var addr = teko::sys::ptr_word(teko::mem::as_cstr(msg))
    _ = teko::sys::syscall3(teko::sys::SYS_WRITE, 1, addr, 3)
    0
}
