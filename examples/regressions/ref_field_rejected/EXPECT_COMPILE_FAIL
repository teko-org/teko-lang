This fixture is a NEGATIVE regression (null-union C3, 0.3.0.30): a `struct` that tries to STORE a
bare `Reference` field. The C3 R2 relaxation admits a `Reference` member ONLY as the never-null
handle of a two-member `Ref<T> | null` union; a bare stored reference field remains an R4 escape-gate
violation. The checker (collect.tks) rejects it, proving the relaxation did not widen into general
reference storage.

EXPECT_STDERR: a reference cannot be stored in a struct/variant/collection
