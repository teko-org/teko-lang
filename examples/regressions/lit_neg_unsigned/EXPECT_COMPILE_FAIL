Negative regression (A): a negative literal cannot be annotated as an unsigned type. (A) carries the
Unary(Minus, Number) rejection through unchanged, so `let x: u8 = -1` stays a compile-time error.
EXPECT_STDERR: a negative literal cannot be annotated as an unsigned type (M.1)
