# Regression (A, NEGATIVE — M.1 preserved): a negative literal cannot be annotated as an unsigned
# type. (A) carries this rejection through unchanged; `let x: u8 = -1` must remain a compile error.
name = "lit_neg_unsigned"
source = "src"

[artifact]
kind = "binary"
