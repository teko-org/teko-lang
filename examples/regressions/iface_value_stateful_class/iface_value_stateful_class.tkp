# Regression (0.3.0.29 interface-value keystone, Crumb S2): a stateful `class & Counter` driven three
# times through a `Counter`-typed param — state persists across the dispatched calls (the class
# arena-mutation rule). C-native only. Expected native exit: 3.
name = "iface_value_stateful_class"
source = "src"

[artifact]
kind = "binary"
