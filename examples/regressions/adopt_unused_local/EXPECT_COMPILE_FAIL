This fixture is a NEGATIVE regression (#337): an unused `let` INSIDE `adopt { }` must be flagged
by `check_locals` — `adopt` is an ordinary control-flow block (unlike `defer`, whose body is
cleanup scratch and is deliberately NOT descended for this diagnostic), so the same
unused-local diagnostic applies inside it. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
