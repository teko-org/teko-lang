# Regression (#337, negative): an unused `let` inside `adopt { }` must be flagged by
# check_locals — adopt is ordinary control flow, not defer-style cleanup scratch.
name = "adopt_unused_local"
source = "src"

[artifact]
kind = "binary"
