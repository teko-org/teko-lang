# Regression (positive, owner ruling 2026-07-15): the keyword-less discard-assign `_ = expr`
# evaluates its RHS for effect and drops the result. It builds and runs; codegen lowers it to
# `(void)(expr);`. Deterministically exits 9 after discarding a real call.
name = "discard_assign_ok"
source = "src"

[artifact]
kind = "binary"
