# A4-5 own-backend differential fixture (issue #385, §9.2). exit(100 - 58) — a SUB
# into x0 before the BL to _tk_exit. VM, C-native, and own-native must all agree on
# exit 42 (scripts/diff_c_own.sh).
name = "own_sub_exit"
source = "src"

[artifact]
kind = "binary"
