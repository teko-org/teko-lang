# Regression (#517, negative): a 3-part loop condition must be boolean. `loop mut i = 0; i; i++ {}`
# folds to `if !i { break }`, and `!i` on an integer is rejected by the checker. No successful build.
name = "loop_three_part_nonbool_cond"
source = "src"

[artifact]
kind = "binary"
