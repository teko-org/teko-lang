pub fn worker(s: str) {
    var expected = "0123456789ABCDEF"
    if s.len != 16 { exit(1) }
    var i: u64 = 0
    loop {
        if i >= s.len { break }
        if s[i] != expected[i] { exit(2) }
        i = i + 1
    }
    exit(0)
}
