Feature: §16 F1 memcpy intrinsic — the spawn arg-pack copies bytes via a byte-loop helper, no <string.h>/libc

  Scenario: spawn_pack_copies_str — main packs a 16-byte str into the spawn context, the worker reads all 16 bytes back and exits 0
    When the program is built and run
    Then it exits 0
