This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9 F multiple
assignment / array decomposition / multiple return REJECT host — four illegal forms live one per
namespace under src/, and the single failing build reports every diagnostic it finds, one per
scenario in the .tkr: a parallel multiple assignment whose target and value counts differ (ruling 1),
binding a multiple-return value to a single name (ruling 3), a second `..` skip-range in an array
decomposition (ruling 2), and an arity-1 `( A )` multiple-return signature (ruling 3). It has no
successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and block_expr_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: the number of targets and values must match
