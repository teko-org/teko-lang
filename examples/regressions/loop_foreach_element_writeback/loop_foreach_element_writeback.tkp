# Regression (#517): the `mut x` for-each binding is a local COPY. A field-write `x.f = v` reaches
# the underlying object ONLY when the element is a CLASS (reference — the copied handle shares the
# arena object); for a STRUCT element it stays on the local copy. Self-checks; VM == native.
name = "loop_foreach_element_writeback"
source = "src"

[artifact]
kind = "binary"
