NEGATIVE regression (owner refinement 2026-07-15): a `_` discard parameter in a plain function has
no interface/base signature to draw its type from, and the author controls the signature — so the
checker rejects it: "a `_` discard parameter is only allowed when implementing an interface method
or overriding a base method". Build-all consumers must skip this dir.
