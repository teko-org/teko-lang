# Regression (negative, owner refinement 2026-07-15): a `_` DISCARD parameter is INVALID in a plain
# function (one whose signature the author controls) — name the parameter instead. Only an
# interface impl / base override may discard a received parameter. Rejected by the checker.
name = "discard_param_plain_fn"
source = "src"

[artifact]
kind = "binary"
