NEGATIVE regression (owner ruling 2026-07-15): `let _ = expr` binds the write-only discard `_`
with a `let` keyword, which is invalid. parse_bind_target rejects it: "`_` is the write-only
discard and cannot be a `let`/`mut`/`const` target". Build all consumers must skip this dir.
