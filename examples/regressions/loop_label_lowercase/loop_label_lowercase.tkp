# Regression (#517, negative): a lowercase loop label (`outer:`) must be REJECTED — the ONLY label
# form is UPPER_SNAKE (`[A-Z][A-Z0-9_]*`), enforced by the checker's check_labels. No successful build.
name = "loop_label_lowercase"
source = "src"

[artifact]
kind = "binary"
