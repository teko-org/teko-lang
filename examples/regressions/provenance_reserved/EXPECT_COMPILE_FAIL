This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the D133 provenance
reserved-name REJECT host — a user unit redefines the reserved base type `str`, which only the
injected base prelude may define. The single failing build reports the reserved-name diagnostic. It
has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker.

EXPECT_STDERR: type 'str' is reserved
