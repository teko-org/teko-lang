// See lambda_opt_typedef.tkp — an optional type used ONLY inside a lambda's body (never
// elsewhere in the enclosing function) must still get its tk_opt_<T> typedef pre-declared
// in the emitted C (#119).
use m

(m::run()) to i32
