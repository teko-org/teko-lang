# Regression (#517): for-each destructure `loop mut { index; value } in <it>` expands (in the
# parser) to per-field `mut index = _elem.index; mut value = _elem.value` copies — NOT the B.13
# path. Field types inferred. Self-checks with exit codes; VM == native.
name = "loop_foreach_destructure"
source = "src"

[artifact]
kind = "binary"
