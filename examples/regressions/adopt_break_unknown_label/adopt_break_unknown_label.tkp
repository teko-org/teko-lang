# Regression (#337, negative): `break nosuchlabel` inside `adopt { }`, naming a label no enclosing
# loop carries, must be REJECTED — adopt threads the SAME label `scope` through unchanged.
name = "adopt_break_unknown_label"
source = "src"

[artifact]
kind = "binary"
