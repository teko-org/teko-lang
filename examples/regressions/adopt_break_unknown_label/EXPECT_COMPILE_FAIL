This fixture is a NEGATIVE regression (#337): a LABELED `break nosuchlabel` inside `adopt { }`
naming a label no enclosing loop carries must be REJECTED by check_labels — adopt passes the same
`scope` through unchanged (it introduces no label frame of its own), so the unknown-label check
still applies inside it. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
