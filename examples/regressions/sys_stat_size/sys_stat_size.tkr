Feature: §16 FASE4 fs — SYS_statx into a per-#os `extern type Statx`, layout pinned by field access

  Scenario: statx_size_of_42_byte_file — write 42 bytes, statx into teko::sys::Statx, assert stx_size == 42, exit 0
    When built and run
    Then exit = 0
