// See lambda_bound_error_union_wrap.tkp — a capturing lambda's bound-error `return e`
// (nested in a match used as a value) must wrap into its OWN union return type, not
// whatever the last-emitted top-level function's return type happened to be (#108).
use m

(m::run()) to i32
