// Exit code = check_ops() = 9 (see src/m/work.tks). Exercises S-JSON decode, encode,
// object_get/array_get, escapes, exponent numbers, nested objects/arrays, and the
// encode(decode(x)) round-trip fixpoint.
use m

(m::check_ops()) to i32
