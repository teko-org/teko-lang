This fixture is a NEGATIVE regression: the compiler MUST reject it. It is the §9.D capability (c)
group-bind REJECT host — two illegal forms live one per namespace under src/, and the single failing
build reports every diagnostic it finds, one per scenario in the .tkr: a group-bind match that leaves a
DIRECT member uncovered (the group `(B | C) as v` covers only the nested member, so `A` stays uncovered),
and a BARE alternation `B | C as v` that tries to bind (parentheses + `as` are the group-bind
discriminator, so the bare alt still cannot bind). It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this marker
(the same rule diagnostics/EXPECT_COMPILE_FAIL and block_expr_reject/EXPECT_COMPILE_FAIL declare).

EXPECT_STDERR: an alternative (`|`) cannot bind; use a separate arm
