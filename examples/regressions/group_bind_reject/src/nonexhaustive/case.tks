type A = struct { a: i64 }

type B = struct { b: i64 }

type C = struct { c: i64 }

fn nonexhaustive(x: A | (B | C)): i64 {
    match x {
        (B | C) as v => match v {
            B as b => b.b
            C as c => c.c
        }
    }
}
