// Exit code semantics (see ../class_destruct_effects.tkp):
//   native -> 7 (Tracker::destruct fires inside use_and_drop and exits)
//   VM     -> 0 (no destructors on the VM — known divergence; falls off the end)
if Tracker::use_and_drop(21) != 42 { exit(1) }
