This fixture is a NEGATIVE regression: the checker MUST reject it (a `Ref<T>` stored as
a struct/collection member — the R4 KEEP-FOREVER escape gate, spine-build-plan §7). It
has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir
containing this marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines
    (its own COMPILE_FAIL list; keep both in sync when adding a negative fixture).

#331 L1 note: this guards that PR-1's pure `fn_spine` query relaxed NOTHING — the R4
collection/struct-member gate (resolve.tks "a reference cannot be stored in a
struct/variant/collection") still rejects, unchanged, on both engines.
