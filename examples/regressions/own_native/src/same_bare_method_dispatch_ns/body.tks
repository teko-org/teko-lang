use sbmd_left
use sbmd_right

pub fn scenario(): i64 {
    var l = sbmd_left::Svc::make(3).tag_of()
    if l != 3 { return -1 }
    var r = sbmd_right::Svc::make(7).tag_of()
    if r != 7 { return -1 }
    l + r
}
