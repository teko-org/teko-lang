pub fn probe(): u64 {
    var present: u64 | null = 40
    var absent: u64 | null = null
    var rp = match present { u64 as v => v; null => 0 to u64 }
    var ra = match absent { u64 as v => v; null => 2 to u64 }
    rp + ra
}
