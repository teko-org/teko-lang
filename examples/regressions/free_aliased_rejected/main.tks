// (#331 L2b) Freeing a target that is ALIASED (`let y = xs; mem::free(xs)`) is the aliased-free UAF
// — `y` would dangle. The spine's affine `mem::free` guard REJECTS it (us = shared). The one real
// native gap (invisible to static analysis AND ASan, only TEKO_MEM_PARANOID) — closed statically.
// Compile-reject on both engines (EXPECT_COMPILE_FAIL). Pairs with examples/regressions/mem_free
// (a UNIQUE free → must keep PASSING at exit 67).
use fa

(fa::run()) to i32
