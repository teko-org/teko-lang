pub fn run() -> i64 {
    mut xs: []i64 = teko::list::empty()
    xs = teko::list::push(xs, 10)
    xs = teko::list::push(xs, 20)
    let y = xs   // ALIAS — y still holds the buffer
    teko::mem::free(xs)   // freeing xs while y aliases it → aliased-free UAF → REJECT
    y[0]
}
