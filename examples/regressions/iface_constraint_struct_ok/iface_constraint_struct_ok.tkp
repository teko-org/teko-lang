# Regression (0.3.0.29 interface-value keystone, Crumb S1): the SAME struct the honest-stop rejects as
# an interface value works through a generic contract bound `<T: Reader>` (static dispatch, no fat
# pointer) — proving the honest-stop names a REAL alternative. C-native only. Expected native exit: 7.
name = "iface_constraint_struct_ok"
source = "src"

[artifact]
kind = "binary"
