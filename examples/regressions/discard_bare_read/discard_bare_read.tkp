# Regression (negative, owner ruling 2026-07-15): the write-only discard `_` cannot appear as an
# R-value — here on an assignment RHS (`y = _`). Rejected at parse time.
name = "discard_bare_read"
source = "src"

[artifact]
kind = "binary"
