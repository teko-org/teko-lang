NEGATIVE regression (owner ruling 2026-07-15): reading the write-only discard `_` on an assignment
RHS (`y = _`) is invalid — `_` carries no value. parse_atom rejects it: "`_` is a write-only
discard and cannot be read". Rejected at parse time.
