# Regression (negative, owner ruling 2026-07-15): the write-only discard `_` cannot be an operand
# in an expression (`_ + 1`). Rejected at parse time.
name = "discard_arith_read"
source = "src"

[artifact]
kind = "binary"
