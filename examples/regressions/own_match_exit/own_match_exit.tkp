# A4-5 own-backend differential fixture (issue #385, §9.2). A match over a constant
# discriminant, each arm diverging via exit(...) — exercises the own backend's
# multi-block match layout (A4-3, RPO after #443). VM, C-native, and own-native must
# all agree on exit 7 (scripts/diff_c_own.sh).
name = "own_match_exit"
source = "src"

[artifact]
kind = "binary"
