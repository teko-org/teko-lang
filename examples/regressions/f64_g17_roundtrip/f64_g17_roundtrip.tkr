Feature: §16 FASE2 float bottom — pure-Teko f64_g17 (%.17g) round-trips every double via parse

  Scenario: battery_round_trips — 0, -0, 0.1, pi, max/min normal, min/max subnormal, +/-inf and nan all round-trip through f64_g17
    When built and run
    Then stdout pattern = "roundtrip ok"
