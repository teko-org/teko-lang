NEGATIVE regression (owner ruling 2026-07-15): passing the write-only discard `_` as a call
argument (`teko::str::concat("a", _)`) reads a value that does not exist. parse_atom rejects the
`_` R-value: "`_` is a write-only discard and cannot be read". Rejected at parse time.
