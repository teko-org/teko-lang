NEGATIVE regression (null-union C5): dereferencing a `Ref<T> | null` before narrowing is rejected by
require_narrowed (the deref fence). No successful build; consumers that build every fixture skip this
dir.
