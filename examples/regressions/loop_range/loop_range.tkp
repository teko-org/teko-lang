# Regression (#517): the range loop head `loop mut i in a..b` / `a..=b`. Proves: exclusive/inclusive
# endpoints, `continue` runs the step (skips i==2), and the endpoint is captured ONCE at entry
# (mutating the bound mid-loop does not extend the range). Self-checks with exit codes; VM == native.
name = "loop_range"
source = "src"

[artifact]
kind = "binary"
