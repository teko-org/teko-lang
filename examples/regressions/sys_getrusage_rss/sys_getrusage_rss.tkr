Feature: §16 FASE4 rusage — peak resident set size via SYS_getrusage in Teko, no libc

  Scenario: peak_rss_positive_after_8mb — after touching 8 MB of pages, peak_rss() is greater than zero, exit 0
    When built and run
    Then exit = 0
