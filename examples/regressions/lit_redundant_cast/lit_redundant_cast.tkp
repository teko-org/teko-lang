# Regression (A, byte-preservation): a redundant `5 to u32` on a u32 binding stays LEGAL — (A) makes
# the cast unnecessary, never illegal. A no-op when the cast target equals the binding's expected
# type. Both engines exit 5.
name = "lit_redundant_cast"
source = "src"

[artifact]
kind = "binary"
