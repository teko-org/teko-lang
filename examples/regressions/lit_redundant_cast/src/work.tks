/**
 * A redundant `5 to u32` on a u32 binding remains legal after (A) — the cast target equals the
 * binding's expected type, so it is a no-op, not an error. Widened to i64 for the exit code.
 *
 * @return 5
 */
pub fn redundant_cast() -> i64 {
    let x: u32 = 5 to u32
    x to i64
}
