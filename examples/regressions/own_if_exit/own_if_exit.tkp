# A4-5 own-backend differential fixture (issue #385, §9.2). A two-block if/else CFG,
# each arm diverging via exit(...) — exercises the own backend's B.cond branch layout
# (A4-3, RPO after #443). VM, C-native, and own-native must all agree on exit 1
# (scripts/diff_c_own.sh).
name = "own_if_exit"
source = "src"

[artifact]
kind = "binary"
