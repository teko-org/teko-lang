# Regression (0.3.0.29 interface-value keystone, Crumb S2): a `class & Reader` up-casts into the
# `Reader` interface VALUE at a call boundary and dispatches through its vtable. `pull(r: Reader, n)`
# drives two dynamic reads and sums them. C-native only (interface layout is unbuilt on the own
# backend, docs/design/interface-value-type.md §1). Expected native exit: 7.
name = "iface_value_param_dispatch"
source = "src"

[artifact]
kind = "binary"
