# Regression (A, NEGATIVE — M.1 preserved): an out-of-range literal in an annotated slot still fails
# EARLY. (A) makes a FITTING literal cast unnecessary; it does NOT relax the range check. `let x: u8
# = 256` must remain a compile error.
name = "lit_u8_overflow"
source = "src"

[artifact]
kind = "binary"
