Negative regression (A): an out-of-range literal in an annotated slot fails early (M.1). (A) borns a
FITTING literal at the annotation's width but never relaxes the range check, so `let x: u8 = 256`
stays a compile-time error.
EXPECT_STDERR: literal out of range for the annotated type (M.1 — fail early)
