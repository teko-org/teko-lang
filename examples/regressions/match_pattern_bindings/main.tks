// Virtual main: call one of the many pattern-binding functions. Reaching it forces the
// WHOLE work.tks (8 binding functions) through the checker — the exact path that used to
// corrupt the heap. A clean exit 5 proves the env-lifecycle fix holds.
use m

m::f5() to i32
