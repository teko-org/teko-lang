// Virtual main: force the whole double-match idiom through both the checker and codegen.
// A clean exit 42 proves the safe idiom (outer case first, then re-match inside it) still
// works after the checker started rejecting the unsafe skip-level shape (#110).
use m

(m::check()) to i32
