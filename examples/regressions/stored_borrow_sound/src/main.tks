// (#331 L2a) A ref bound to a LOCAL (`let r = p`, p a `Ref<i64>` param) is now ADMITTED by the
// spine's Ref-storability gate — it was unconditionally rejected before. Exit = sb::run() = 70:
// three sound ref-to-local shapes (top-level bind, inside a loop body, inside a match arm), each
// mutating its param through the local ref handle. Proves the relaxation is FUNCTIONAL (the produced
// native binary exits 70, the EXPECT_EXIT marker), not merely accepted.
use sb

(sb::run()) to i32
