This fixture is a NEGATIVE / DEFERRED regression (#517): a raw collection is not yet iterable. A
for-each `loop mut x in xs` desugars to a closure-call `_it()`; a `[]i64` slice is not callable, so
it is REJECTED by the checker. Raw-collection for-each is a named follow-on (needs IntoIterator +
a usable generic Iterator<T>). It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
