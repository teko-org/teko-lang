# Regression (#517, negative): destructuring a field the yielded element does not have. The parser
# expands `{ nope }` to `mut nope = _elem.nope`; the checker rejects the unknown field. No build.
name = "loop_foreach_destructure_unknown_field"
source = "src"

[artifact]
kind = "binary"
