This fixture is a NEGATIVE regression (#337): a `return` INSIDE an `adopt { }` block must be
type-checked against the enclosing function's declared return type, exactly like a `return`
anywhere else — `adopt` allows return/break/continue (unlike `defer`), so this path must still be
checked. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
