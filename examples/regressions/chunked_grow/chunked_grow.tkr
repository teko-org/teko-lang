Feature: Chunked<T> grow-without-recopy container for the IR builders (D207)

  Scenario: push_beyond_two_chunks — appending past 2*CHUNK keeps order, len, first/last, get and iteration
    When the program is built and run
    Then it exits 0
