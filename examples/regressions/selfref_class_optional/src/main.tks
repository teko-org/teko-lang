// See selfref_class_optional.tkp — a self-referencing class-optional field (`next: Node?`)
// built into a linked list and summed. VM and native must agree (exit = sum = 6).
use list

(list::run()) to i32
