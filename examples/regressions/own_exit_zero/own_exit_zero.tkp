# A4-5 own-backend differential fixture (issue #385, §9.2). The minimal program:
# an explicit exit(0). VM, C-native, and own-native (TEKO_BACKEND=native) must all
# agree on exit 0 — the first light of the C-vs-own differential (scripts/diff_c_own.sh).
name = "own_exit_zero"
source = "src"

[artifact]
kind = "binary"
