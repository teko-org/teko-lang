type Animal = virtual class {
    pub name: str

    pub fn make(n: str) -> Animal {
        Animal { name = n }
    }

    pub virtual fn speak(self) -> str {
        "..."
    }

    pub fn greet(self) -> str {
        self.speak()
    }
}
