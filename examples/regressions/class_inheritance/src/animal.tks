type Animal = virtual class {
    name: str

    fn make(n: str) -> Animal {
        Animal { name = n }
    }

    virtual fn speak(self) -> str {
        "..."
    }

    fn greet(self) -> str {
        self.speak()
    }
}
