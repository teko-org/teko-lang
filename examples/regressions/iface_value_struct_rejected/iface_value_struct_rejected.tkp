# NEGATIVE regression (0.3.0.29 interface-value keystone, Crumb S1): a `struct` conforming to `Reader`
# passed where the interface VALUE is required — rejected with a NAMED honest-stop (a struct has no
# stable address to back a fat pointer). See EXPECT_COMPILE_FAIL for the pinned message substrings.
name = "iface_value_struct_rejected"
source = "src"

[artifact]
kind = "binary"
