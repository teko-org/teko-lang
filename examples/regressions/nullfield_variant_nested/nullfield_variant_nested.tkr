Feature: nullfield_variant_nested — a `variant | null` field at the inner level of a nested struct

  # `Outer { inner: Inner; tag }` embeds `Inner { f: Op | null }` by value, so the inner union must be
  # emitted before both structs. Reading the two-level path `outer.inner.f` and matching it returns
  # `b.y + tag` = 32 + 10 = 42.

  Scenario: read and match a null-union field nested one struct deep
    When built and run
    Then exit = 42
