This fixture is a NEGATIVE regression: the checker MUST reject it (a `private` module-level const,
declared in namespace `m1`, referenced from the ROOT namespace via the namespace-qualified value
spelling `m1::PRIV` — the const's DECLARING namespace, not the reference's, gates `private` reach;
D4/D6, #594 crumb 8c / issue #613). It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/compile_fail_regressions.sh — builds it and asserts the build FAILS (issue #610).

EXPECT_STDERR: is private to
