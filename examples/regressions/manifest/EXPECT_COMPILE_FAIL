This fixture is a NEGATIVE regression: the MANIFEST READER must reject it. The rejected construct is
in the `.tkp` itself (`kind = "binari"`), not under src/, so the build fails before any `.tks` is
lexed. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/compile_fail_regressions.sh — builds it and asserts the build FAILS (issue #610).

EXPECT_STDERR: unknown [artifact] kind "binari"
