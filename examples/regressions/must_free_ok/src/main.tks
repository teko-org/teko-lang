use must_free_ok::ops

let a = ops::straight_line_free()
let b = ops::if_else_both_free(true)
let c = ops::match_all_arms_free(0)
let d = ops::diverging_guard_then_free(false)

(a + b + c + d) to i32
