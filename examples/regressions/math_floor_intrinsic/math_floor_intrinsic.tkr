Feature: §16 F1 math floor intrinsic — teko::floor lowers to a soft-float helper, no <math.h>/libm

  Scenario: floor_soft_matches_libc — the emitted tk_floor computes floor over positive and negative doubles
    When the program is built and run
    Then it exits 0
