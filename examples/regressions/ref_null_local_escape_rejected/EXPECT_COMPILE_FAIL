NEGATIVE regression (null-union C5): a function may not RETURN a `Ref<T> | null` — references are
pass-down only (R3). type_function rejects a ref-bearing `Ref<T> | null` return type. It has no
successful build to sanitize/run; consumers that build every fixture must skip this dir.
