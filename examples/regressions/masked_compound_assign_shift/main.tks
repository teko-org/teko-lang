// Virtual main: exit code = m::masked_compound_shift_total() = 385 truncated to a byte = 129.
// See masked_compound_assign_shift.tkp for the full derivation.
use m

(m::masked_compound_shift_total()) to i32
