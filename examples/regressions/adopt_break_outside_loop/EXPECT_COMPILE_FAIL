This fixture is a NEGATIVE regression (#337): a bare `break` inside `adopt { }` with NO enclosing
loop must be REJECTED by check_labels — `adopt` introduces no loop/label frame of its own (it is
an ordinary block, not a loop), so a break inside it with nothing enclosing must still fail the
same way a bare top-level `break` would. It has no successful build to sanitize/run.

Consumers of examples/regressions/* that BUILD every fixture must skip any dir containing this
marker:
  - .github/workflows/sanitizers.yml (the native-build-all-fixtures loop) — skips it.
  - scripts/diff_vm_native.sh — asserts it fails to compile on BOTH engines (its own
    COMPILE_FAIL list; keep both in sync when adding a negative fixture).
