# Regression (#337, negative): a bare `break` inside `adopt { }` with NO enclosing loop must be
# REJECTED — adopt introduces no loop/label frame of its own, so `in_loop` is unchanged by it.
name = "adopt_break_outside_loop"
source = "src"

[artifact]
kind = "binary"
