This fixture is a NEGATIVE regression (null-union C4, 0.3.0.30): `#inline` attaches to a local binding
or a struct field, NOT a declaration. Placing it before a `fn` at declaration position is a precise
placement error (the decl-attribute parser names the two legal positions), never a silent accept or a
mis-parse.

EXPECT_STDERR: `#inline` attaches to a local binding
