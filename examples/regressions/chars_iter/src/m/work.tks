// abc_len — returns the number of UTF-8 codepoints in "abc" via the len_chars builtin.
// Expected: 3 (three ASCII codepoints). Forces len_chars(str)->i64 through checker+VM+codegen.
pub fn abc_len() -> i64 { len_chars("abc") }
