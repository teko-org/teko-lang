// Virtual main: exit code = abc_len() == len_chars("abc") == 3.
// A clean exit 3 on BOTH the VM (`teko run`) and the compiled binary (`teko build`)
// proves the len_chars builtin holds end-to-end.
use m

(m::abc_len()) to i32
