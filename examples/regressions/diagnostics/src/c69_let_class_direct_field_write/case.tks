type Named = class {
    pub name: str
    pub fn of(n: str): Named {
        Named { name = n }
    }
}

pub fn probe(): i64 {
    var a = Named::of("a")
    a.name = "b"
    a.name.len to i64
}

pub fn entry() {
    (probe()) to i32
}
