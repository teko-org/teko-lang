pub fn masked_compound_shift_total(): i32 {
    var a: i32 = 1 to i32
    a <<= 40

    var b: u8 = 1 to u8
    b <<= 200

    var c: u64 = 256 to u64
    c >>= 65

    var d: u8 = 1
    d <<= 200

    a + b + c + (d to i32)
}
pub fn entry() {
    (masked_compound_shift_total())
}
