pub extern fn c_getenv_bare(name: ptr<byte>): ptr<byte> | null = "getenv"

pub fn dereference_without_check(name: ptr<byte>): u64 {
    var value: ptr<byte> = c_getenv_bare(name)
    teko::mem::bytes_from_ptr(value, 1).len
}
