Feature: §16 FASE2 SYS_exit_group — a raw exit_group syscall sets the process status directly, no libc exit

  Scenario: exit_group_sets_status_42 — syscall1(SYS_EXIT_GROUP, 42) ends the process at 42, never reaching the exit(99) fallback
    When built and run
    Then exit = 42
