# Regression (#517): the extensible-loop frontend — a while-head counting loop, a bare-ident
# while-condition (`loop go {…}` treats `go` as a bool, NOT a label), and a prefix-labeled loop
# with a labeled break from an inner loop. Self-checks with exit codes; VM == native (exit 0).
name = "loop_while_and_label"
source = "src"

[artifact]
kind = "binary"
