# A4-5 own-backend differential fixture (issue #385, §9.2). exit(42) — a nonzero
# exit code carried through the own-backend BL to _tk_exit. VM, C-native, and
# own-native must all agree on exit 42 (scripts/diff_c_own.sh).
name = "own_exit_code"
source = "src"

[artifact]
kind = "binary"
