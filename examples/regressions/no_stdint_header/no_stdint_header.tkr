Feature: §16 F1 no stdint header — fixed-width types lower to codegen typedefs and a zero-fill byte-loop, no <stdint.h>/<stdbool.h>/libc

  Scenario: fixed_width_typedefs_and_zero_fill — u8/u32/i32 values and a `[16]u8 = []` zero-fill compute correctly with no C header
    When the program is built and run
    Then it exits 0
