Feature: §16 FASE4 fs — directory listing via SYS_getdents64 parsed in Teko, no libc

  Scenario: getdents_root_contains_usr — list "/" via getdents64, assert an entry named "usr" is present, exit 0
    When built and run
    Then exit = 0
