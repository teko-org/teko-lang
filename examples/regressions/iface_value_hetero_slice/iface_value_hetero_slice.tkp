# Regression (0.3.0.29 interface-value keystone, Crumb S2): a heterogeneous `[]Shape` holds distinct
# conforming classes and dispatches `area()` per element through each vtable. C-native only.
# Expected native exit: 0.
name = "iface_value_hetero_slice"
source = "src"

[artifact]
kind = "binary"
