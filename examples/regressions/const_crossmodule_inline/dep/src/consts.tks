/**
 * The dep's `pub` scalar module const — surfaced bare (unqualified) to a consuming
 * module via `seed_from_dep`/`seed_dep_consts` (#594 crumb 8) and substituted by
 * `inline_consts` at the consumer's use site, never a cross-module data symbol.
 */
pub const P: i64 = 20

/**
 * The dep's `exp` scalar module const — same cross-module surfacing as `P` (`exp` and
 * `pub` are both exported visibilities for a module-level const, #594 crumb 8).
 */
exp const Q: i64 = 22
