// (W10b.CLASS residual -- intern visibility) `intern name: str` on Animal is reachable from
// Dog (a subclass) via own_name(), but NOT from outside any class (would be a compile error
// if uncommented: `d.name` here).
let d = Dog::make("Rex")
if d.own_name() != "Rex" { exit(1) }
