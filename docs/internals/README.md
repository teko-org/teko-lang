# The internals

How the port itself is built: `teko.tk` and the 30 `teko_*.tk` modules (31 module
files in all), the pass model, reference counting, the bootstrap ladder, CI, and known
pitfalls. The pages are being written,
subject by subject; until they land, [`../../CONTRIBUTING.md`](../../CONTRIBUTING.md) is
the operational guide, and the taught compiler's own modules are the account of record.

Planned pages: module map (`core_teko.mc`/`user.mc` and the 31 `.tk` modules), the pass
model, reference counting, the bootstrap ladder, CI, pitfalls, and known debts.
