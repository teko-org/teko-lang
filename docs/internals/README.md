# The internals

How the port itself is built: the 31 `teko_*.tk` modules, the pass model, reference
counting, the bootstrap ladder, CI, and known pitfalls. Pages land in
[D4](../history/design/plano-docs-site.md) of the docs plan, rewritten by subject from
[`../history/handoff-2026-09.md`](../history/handoff-2026-09.md), which is the full
operational guide until then.

Planned pages: module map (`core_teko.mc`/`user.mc` and the 31 `.tk` modules), the pass
model, reference counting, the bootstrap ladder, CI, pitfalls, and known debts.
