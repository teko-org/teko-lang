# E4/X1 — cross-compiling (skeleton)

Placeholder da mini-umbrella do #457. Escopo ratificado no issue: seleção de
alvo (`--target`), archives de runtime por alvo (#182), determinismo
cross==native (byte-identity M.2), e o pooling de testes em 3 camadas
(byte-identity → qemu → matriz cruzada real). O crumb plan do arquiteto
substitui este arquivo quando o E4 abrir (após E1–E3).
