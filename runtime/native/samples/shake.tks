// Phase 13 native runner — SHAKE128/256 XOF executable proof (FIPS 202, empty message).
// hash.shake128/256(msg, outLen) -> outLen bytes squeezed, as hex.
extern fn emit(s: cstr) from "teko_rt" as "teko_rt_emit";

emit(hash.shake128("", 32));
emit(hash.shake256("", 32));
