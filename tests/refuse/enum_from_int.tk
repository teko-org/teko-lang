// enum_from_int.tk -- an enum does not accept a plain integer; only its own
// members convert to it.
//
// expect-refuse: teko: a value of type i64 does not convert to Color
// expect-refuse-line: 9

enum Color { Red, Green, Blue }
i64 main() {
    Color c = 1;
    return 42;
}
