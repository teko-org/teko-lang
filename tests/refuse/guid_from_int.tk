// guid_from_int.tk -- docs/specs/guid.md § 2: a `Guid` slot takes a `Guid` and
// nothing else. It is a `type_new` id outside `tk_is_int_ty` and outside the
// struct table, so the clause docs/specs/datetime.md § 5 wrote for a primitive
// with members is what answers (teko_typeof.tk).
//
// expect-refuse: teko: a value of type i64 does not convert to Guid
// expect-refuse-line: 12

#include "guid.tk"

i64 main() {
    Guid g = 0;
    return 0;
}
