// string_index_write.tk -- docs/specs/string.md § 6/§ 10, D89: a `string`
// is immutable, C#'s own rule and teko's -- `s[i] = c` is refused rather
// than lowered to a store `tk_string_at` (N8, lib/string.tk) has no
// counterpart for.
//
// expect-refuse: teko: a string is immutable
// expect-refuse-line: 13
#include "../../lib/rt.tk"
#include "../../lib/string.tk"

i64 main() {
    string s = new string("hi");
    s[0] = 'H';
    return 0;
}
