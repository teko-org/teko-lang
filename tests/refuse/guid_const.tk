// guid_const.tk -- docs/specs/guid.md § 2, the same row `decimal` earns
// (tests/refuse/decimal_const.tk): the constant folder has no 128-bit
// arithmetic, so a sixteen-byte value is no constant expression. The wording is
// the folder's own, which is what `decimal` already measured (teko_const.tk).
//
// expect-refuse: teko: const requires a constant expression
// expect-refuse-line: 11

#include "guid.tk"

const Guid ID = Guid.Empty;

i64 main() {
    return 0;
}
