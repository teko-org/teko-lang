// struct_no_new.tk -- a struct local read through before anything is ever
// written to it is refused, same as a plain scalar (D46).
//
// expect-refuse: teko: v is used before it is assigned
// expect-refuse-line: 10

struct Vec { public i64 x; }
i64 main() {
    Vec v;
    return v.x;
}
