// string_str_implicit.tk -- docs/specs/string.md § 3 rule 4/§ 5: N7b (D88)
// interns a `"..."` LITERAL written where a `string` is expected (§ 4,
// `tests/surface_string_value.tk`/`_intern.tk`), but a `str` VARIABLE --
// even one a literal initialized two lines earlier -- is not itself a
// literal at the point it is read, and stays refused: `new string(p)` is
// the explicit road the page names. The generic "does not convert" row
// every class/`uptr` mismatch already answers with, unmoved by N7b.
//
// expect-refuse: teko: a value of type uptr does not convert to string
// expect-refuse-line: 16
#include "../../lib/rt.tk"
#include "../../lib/string.tk"

i64 main() {
    str p = "raw";
    string v = p;
    return 0;
}
