// type_word_deleg.tk -- a `delegate` whose name is already a registered
// TYPE WORD -- `TimeSpan`, a teko primitive (teko_time.tk) -- is refused,
// not silently accepted with the primitive shadowed program-wide (D60).
//
// expect-refuse: teko: the name is already a type: TimeSpan
// expect-refuse-line: 8

delegate i64 TimeSpan(i64 x);

i64 main() { return 0; }
