// prim_tostring_f32.tk -- D98: `f32` is refused for the same reason `f64` is,
// and by the same door.
//
// expect-refuse: teko: f32 has no members: ToString
// expect-refuse-line: 11
#include "../../lib/rt.tk"
#include "../../lib/string.tk"

i64 main() {
    f32 x = 3.5;
    string s = x.ToString();
    return 0;
}
