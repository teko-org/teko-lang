// type_word_struct.tk -- a `struct` whose name is already a registered TYPE
// WORD -- `DateTime`, a teko primitive (teko_time.tk) -- is refused, not
// silently accepted with the primitive shadowed program-wide (D60).
//
// expect-refuse: teko: the name is already a type: DateTime
// expect-refuse-line: 8

struct DateTime {
    public i64 v;
}

i64 main() { return 0; }
