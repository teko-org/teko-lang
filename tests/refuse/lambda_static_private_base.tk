// expect-refuse: teko: B.sec is private
// expect-refuse-line: 7
#include "../../lib/rt.tk"
delegate i64 Op(i64 a);
class B { private static i64 sec; }
class D : B {
    public static i64 peek() { Op f = (i64 x) => sec + x; return f(1); }
}
i64 main() { return D.peek(); }
