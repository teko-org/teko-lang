// string_interp_no_formatter.tk -- docs/specs/string.md § 9, N10: a hole's
// static type with no formatter is refused by name -- `f64`, no `fmt_f64`
// anywhere in this tree (measured), § 9's own "not taught".
//
// expect-refuse: teko: no interpolation of a value of type f64
// expect-refuse-line: 12
#include "../../lib/rt.tk"
#include "../../lib/string.tk"

i64 main() {
    f64 x = 1.5;
    string s = $"{x}";
    i64 len = s.Length;
    return len;
}
