// type_word_core.tk -- a `class` whose name is already `f64`, mc's own core
// type word (teko_float.tk, mc's bundled `<float>` module): the same
// refusal a teko primitive's own word gets (D60), because both are a
// `type_new`-registered word to `tk_newname`.
//
// expect-refuse: teko: the name is already a type: f64
// expect-refuse-line: 9

class f64 {
    public i64 v;
}

i64 main() { return 0; }
