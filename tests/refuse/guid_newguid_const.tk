// guid_newguid_const.tk -- N9, D91, ruling 3: `Guid.NewGuid()` is now a
// legal CALL (tests/primitives_guid_newguid.tk), and a call is still no
// constant expression -- the same folder gap `guid_const.tk` already
// measures for `Guid.Empty`, restated here so landing N9 is not read as
// having opened a road around it.
//
// expect-refuse: teko: const requires a constant expression
// expect-refuse-line: 12

#include "guid.tk"

const Guid ID = Guid.NewGuid();

i64 main() {
    return 0;
}
