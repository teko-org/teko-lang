// type_word_iface.tk -- an `interface` whose name is already a registered
// TYPE WORD -- `DateOnly`, a teko primitive (teko_time.tk) -- is refused,
// not silently accepted with the primitive shadowed program-wide (D60).
//
// expect-refuse: teko: the name is already a type: DateOnly
// expect-refuse-line: 8

interface DateOnly {
    i64 v();
}

i64 main() { return 0; }
