// i128_const.tk -- N6a of docs/specs/small-ints.md (§ 9; D81): an `i128` is
// not a `const`, an array size or a `switch` label, for the reason `decimal`
// is none of those either -- the core's folder has no 128-bit arithmetic and
// the literal is the `N_IDENT` of its own blob global, not a folded value.
// The refusal is the core's own wording, reached unchanged.
//
// expect-refuse: teko: const requires a constant expression
// expect-refuse-line: 12

#include "wide.tk"

const i128 K = 1i;

i64 main() {
    return 0;
}
