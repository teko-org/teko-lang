// type_word_alias.tk -- a `class` whose name is already `usize`, one of the
// seven `type_alias` words teko itself declares (teko_type.tk): the same
// refusal a `type_new`-registered word gets (D60), checked against teko's
// own list because an alias leaves no row in mc's type table to count.
//
// expect-refuse: teko: the name is already a type: usize
// expect-refuse-line: 9

class usize {
    public i64 v;
}

i64 main() { return 0; }
