// guid_unknown_member.tk -- docs/specs/guid.md § 2: a primitive answers every
// member by name, out of its own row table and never out of a vtable
// (teko_prim.tk).
//
// expect-refuse: teko: unknown member of Guid: Version
// expect-refuse-line: 12

#include "guid.tk"

i64 main() {
    Guid g = Guid.Empty;
    i64 n = g.Version;
    return n;
}
