// expect-refuse: teko: the member is a property; it is not called: Side
// expect-refuse-line: 8
#include "../../lib/rt.tk"
delegate i64 Op(i64 a);
i64 Side(i64 x) { return x + 100; }
class H {
    public i64 Side { get { return 3; } }
    public i64 go() { Op f = (i64 x) => Side(x); return f(1); }
}
i64 main() { H h = new H; return h.go(); }
