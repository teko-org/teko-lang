// enum_plus.tk -- `+` is not an operator an enum's members take.
//
// expect-refuse: teko: no operator `+` takes these operands
// expect-refuse-line: 9

enum Color { Red, Green, Blue }
i64 main() {
    Color c = Color.Red;
    Color d = c + c;
    return 42;
}
