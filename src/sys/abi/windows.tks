#os("windows")
pub extern fn MultiByteToWideChar(cp: u32, oflags: u32, mbstr: u64, cb: i32, wstr: u64, cch: i32): i32 = "MultiByteToWideChar" from "kernel32"

#os("windows")
pub extern fn CreateFileW(name: u64, access: u32, share: u32, sec: u64, disp: u32, oflags: u32, tmpl: u64): u64 = "CreateFileW" from "kernel32"

#os("windows")
pub extern fn CreateFileA(name: u64, access: u32, share: u32, sec: u64, disp: u32, open_flags: u32, template: u64): u64 = "CreateFileA" from "kernel32"

#os("windows")
pub extern fn ReadFile(h: u64, buf: u64, n: u32, got: u64, ov: u64): i32 = "ReadFile" from "kernel32"

#os("windows")
pub extern fn WriteFile(h: u64, buf: u64, n: u32, put: u64, ov: u64): i32 = "WriteFile" from "kernel32"

#os("windows")
pub extern fn SetFilePointerEx(h: u64, dist: i64, newp: u64, method: u32): i32 = "SetFilePointerEx" from "kernel32"

#os("windows")
pub extern fn GetFileSizeEx(h: u64, size: u64): i32 = "GetFileSizeEx" from "kernel32"

#os("windows")
pub extern fn CloseHandle(h: u64): i32 = "CloseHandle" from "kernel32"

#os("windows")
pub extern fn GetStdHandle(which: u32): u64 = "GetStdHandle" from "kernel32"

#os("windows")
pub extern fn GetSystemTimePreciseAsFileTime(ft: u64) = "GetSystemTimePreciseAsFileTime" from "kernel32"

#os("windows")
pub extern fn QueryPerformanceCounter(c: u64): i32 = "QueryPerformanceCounter" from "kernel32"

#os("windows")
pub extern fn QueryPerformanceFrequency(f: u64): i32 = "QueryPerformanceFrequency" from "kernel32"

#os("windows")
pub extern fn CreateProcessA(app: u64, cmd: u64, proc_attr: u64, thread_attr: u64, inherit: i32, create_flags: u32, env: u64, cur_dir: u64, startup: u64, proc_info: u64): i32 = "CreateProcessA" from "kernel32"

#os("windows")
pub extern fn WaitForSingleObject(handle: u64, ms: u32): u32 = "WaitForSingleObject" from "kernel32"

#os("windows")
pub extern fn GetExitCodeProcess(handle: u64, code: u64): i32 = "GetExitCodeProcess" from "kernel32"

#os("windows")
pub extern fn FindFirstFileA(name: u64, data: u64): u64 = "FindFirstFileA" from "kernel32"

#os("windows")
pub extern fn FindNextFileA(h: u64, data: u64): i32 = "FindNextFileA" from "kernel32"

#os("windows")
pub extern fn FindClose(h: u64): i32 = "FindClose" from "kernel32"

#os("windows")
pub extern fn CreateDirectoryA(path: u64, sec: u64): i32 = "CreateDirectoryA" from "kernel32"

#os("windows")
pub extern fn DeleteFileA(path: u64): i32 = "DeleteFileA" from "kernel32"

#os("windows")
pub extern fn GetFileAttributesA(path: u64): u32 = "GetFileAttributesA" from "kernel32"

#os("windows")
pub extern fn GetEnvironmentStringsW(): u64 = "GetEnvironmentStringsW" from "kernel32"

#os("windows")
pub extern fn FreeEnvironmentStringsW(p: u64): i32 = "FreeEnvironmentStringsW" from "kernel32"

#os("windows")
pub extern fn GetCurrentDirectoryA(len: u32, buf: u64): u32 = "GetCurrentDirectoryA" from "kernel32"

#os("windows")
pub extern fn SetCurrentDirectoryA(path: u64): i32 = "SetCurrentDirectoryA" from "kernel32"

#os("windows")
pub extern fn VirtualAlloc(addr: u64, size: u64, alloc_type: u32, protect: u32): u64 = "VirtualAlloc" from "kernel32"

#os("windows")
pub extern fn VirtualFree(addr: u64, size: u64, free_type: u32): i32 = "VirtualFree" from "kernel32"

#os("windows")
pub extern fn VirtualProtect(addr: u64, size: u64, new_prot: u32, old_prot: u64): i32 = "VirtualProtect" from "kernel32"

#os("windows")
pub extern fn ExitProcess(code: u32) = "ExitProcess" from "kernel32"
