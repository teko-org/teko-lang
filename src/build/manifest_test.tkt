// src/build/manifest_test.tkt — tests for teko::build manifest reader. #test collected.
// (Uses bare `assert`, the current .tkt convention; teko::assert migration is crumb B2.)

// did the parse succeed?
fn parses(src: str) -> bool {
    match parse_manifest(src) { Manifest => true; error => false }
}

#test
fn parses_executable_manifest() {
    // name + source + `[artifact] kind = "binary"` → Executable.
    let src = "name = \"app\"\nsource = \"src\"\n[artifact]\nkind = \"binary\"\n"
    match parse_manifest(src) {
        Manifest as m => {
            assert m.name == "app"
            assert m.source == "src"
            assert m.artifact == Artifact::Executable
        }
        error => assert false
    }
}

#test
fn parses_library_manifest() {
    // no `kind = "binary"` (or kind = "library") → Library.
    let src = "name = \"lib\"\nsource = \"src\"\n[artifact]\nkind = \"library\"\n"
    match parse_manifest(src) {
        Manifest as m => {
            assert m.name == "lib"
            assert m.artifact == Artifact::Library
        }
        error => assert false
    }
}

#test
fn parses_deps_and_aliases() {
    let src = "name = \"app\"\nsource = \"src\"\n[dependencies]\nhttp = \"1.0\"\njson = \"2.0\"\n[aliases]\nio = \"teko::io\"\n"
    match parse_manifest(src) {
        Manifest as m => {
            assert m.deps.len == 2
            assert m.aliases.len == 1
        }
        error => assert false
    }
}

#test
fn rejects_malformed() {
    // a missing required field → honest error (M.3).
    assert !parses("source = \"src\"\n")              // no name
    assert !parses("name = \"app\"\n")                // no source
    // a malformed line → honest error.
    assert !parses("name \"app\"\nsource = \"src\"\n") // missing '='
    // a valid one still parses.
    assert parses("name = \"app\"\nsource = \"src\"\n")
}
